argumento o parámetro es lo mismo.
buscar public;
static;
void significa que no retorna nada.
agregar lo del metodo sort al resumen de visual de repaso unidad 1,2y3

el to.String convierte los elementos del array en un String ya que si el método .sort solo ordena elementos que son String dado a entender que el método .sort solo funciona con strings de arrays. pagina 16
página 17 no ver.
arrays.sort se utiliza para el ordenamiento de datos primitivos y collection.sort se puede utilizar para ordenar objetos cómo los arraylist. etc.
clase String, archivo Streams, Strings y codifiación en java
lo que está en amarillo hay que estudiarlo.
todos los Strings de una aplicación de java, es una instancia de la clase String.

una instancia de una clase se genera en tiempo de ejecución.

patrón singleton.
